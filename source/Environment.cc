/*
 * Environment.cc
 */

#include "Environment.h"
#include <string>

using namespace std;

Environment::Environment(double xp, double yp, double xs, double ys, string file_name)
    : Drawable(xp, yp, xs, ys, file_name)
{
}
